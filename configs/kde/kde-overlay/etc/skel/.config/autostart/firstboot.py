
#!/bin/sh
if [ -f ~/.config/autostart/turbulence.desktop ]; then
    #Removes the autostart file, so it won't start again at first boot.
    rm ~/.config/autostart/turbulence.desktop 
fi
#end / for turbulence
 
