#
# Place in /home/[user account]/.config/openbox/autostart as user template
# Otherwise, can be placed in /etc/xdg/openbox/autostart as global template
# Written by the Manjaro Team
#

# xdg-user-dirs-update check if user has directories document,download ...
# If not, create them and copy Manjaro Documents to ~/Documents
if [ -x /usr/bin/xdg-user-dirs-update ]; then
    /usr/bin/xdg-user-dirs-update
    . ~/.config/user-dirs.dirs
    cp /etc/manjaro/documents/* $XDG_DOCUMENTS_DIR
fi

# Authentication Agent
/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &

# Background
nitrogen --restore &

# Compositing
xcompmgr &

# Startup
nm-applet &
pnmixer &
xscreensaver -no-splash &
udiskie &
synapse &

# Panel
tint2 &
(sleep 1s && tint2 -c ~/.config/tint2/tint2-workspaces) &

